function victim(num){
    var elem = [];
    var vic;
    for(var i = 0; i < num; i++){
        vic = prompt("");
        elem.push(vic);
    }
    // var rendom = Math.floor(Math.rendom * elem.length)
    console.log(elem.length);
}